cinsrto saveText = %*_text_*%;
cinsrto saveData = %*_save_*%;
cinstro fontData = %*_get_*%;
cinstro get = %*_font_*%;

function all{
  defaultFont("system");
  SAVECONNECT
}

function main{
  varSupport = titleVault, uxVault, uiVault, guiVault;
  title("Leva Note");
  loadStyle(style.css)
  design(uxVault, uiVault, guiVault);
  editTemplate("" save-text:"editTemplate");
  defaultFont("system" default-box:all);
  favicon(icon.ico);
  SAVECONNECT
}
